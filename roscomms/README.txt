README.txt

This library is an experimental attempt to mimic the mbot_firmware/comms library using ROS instead.  
Ideally, current comms functionality would be replicated.  I have no real clue if that's possible but I want to try.